{
  "name": "@veoable/mcp-server",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --external @modelcontextprotocol/sdk --external @veoable/schema --external @veoable/plugin-api --external @veoable/graph-db --external @veoable/flow-stitcher --external @veoable/skill",
    "dev": "tsup src/index.ts --format esm --dts --external @modelcontextprotocol/sdk --external @veoable/schema --external @veoable/plugin-api --external @veoable/graph-db --external @veoable/flow-stitcher --external @veoable/skill --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@veoable/flow-stitcher": "workspace:*",
    "@veoable/graph-db": "workspace:*",
    "@veoable/plugin-api": "workspace:*",
    "@veoable/schema": "workspace:*",
    "@veoable/skill": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
