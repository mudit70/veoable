{
  "name": "@veoable/graph-db",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --external @veoable/schema --external @veoable/plugin-api",
    "dev": "tsup src/index.ts --format esm --dts --external @veoable/schema --external @veoable/plugin-api --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@veoable/plugin-api": "workspace:*",
    "@veoable/schema": "workspace:*",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "tsup": "^8.0.0",
    "typescript": "^6.0.3",
    "vitest": "^2.0.0"
  }
}
