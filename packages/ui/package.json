{
  "name": "@veoable/ui",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'UI build not yet configured (will use Vite)'",
    "dev": "echo 'UI dev not yet configured (will use Vite)'"
  },
  "dependencies": {
    "@veoable/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
