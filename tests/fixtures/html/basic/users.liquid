<!DOCTYPE html>
<html>
<body>
  <h1>{{ page_title }}</h1>
  <form action="/api/users" method="POST">
    <button type="submit">Create</button>
  </form>
  {% for user in users %}
    <div>{{ user.name }}</div>
  {% endfor %}
</body>
</html>
